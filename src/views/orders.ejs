<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <title>我的订单 - 电子商务网站实验</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="container mt-4">
      <div class="mb-3">
        <a href="/" class="btn btn-link">&larr; 返回首页</a>
      </div>
      <h1 class="mb-3">我的订单</h1>
      <% if (!orders.length) { %>
      <p>暂无订单。</p>
      <% } else { %>
      <table class="table table-striped">
        <thead>
          <tr>
            <th>订单号</th>
            <th>状态</th>
            <th>总金额</th>
            <th>创建时间</th>
          </tr>
        </thead>
        <tbody>
          <% orders.forEach(o => { %>
          <tr>
            <td><%= o.id %></td>
            <td><%= o.status %></td>
            <td>￥<%= o.total_amount.toFixed(2) %></td>
            <td><%= o.created_at %></td>
          </tr>
          <% }) %>
        </tbody>
      </table>
      <% } %>
    </div>
  </body>
</html>


