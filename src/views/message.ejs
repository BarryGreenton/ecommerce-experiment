<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <title><%= title || '提示' %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="container mt-5" style="max-width: 540px">
      <div class="card">
        <div class="card-body">
          <h4 class="card-title mb-3"><%= title || '提示' %></h4>
          <p class="card-text"><%= message %></p>
          <a class="btn btn-primary" href="<%= backUrl || '/' %>">返回</a>
        </div>
      </div>
    </div>
  </body>
</html>


